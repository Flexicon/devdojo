package problems

/**
 * Merge every pair of overlapping closed intervals and return the resulting
 * intervals sorted by start. For example, [[1,3], [2,6], [8,10]] becomes
 * [[1,6], [8,10]]. Intervals that only touch at an endpoint overlap.
 * Constraints: 0 <= intervals.size <= 10_000; each interval has two values.
 * Difficulty: medium. Tags: sorting, intervals.
 */
fun mergeIntervals(intervals: List<IntArray>): List<IntArray> = TODO("implement me")
