package problems

/**
 * Given a list of integers and a target, return the zero-based indices of the
 * two distinct elements whose values add up to the target.
 *
 * You may assume exactly one solution exists. Return the indices in ascending
 * order. For example, [2, 7, 11, 15] with target 9 returns (0, 1).
 * Constraints: 2 <= numbers.size <= 10_000; values and target fit in Int.
 * Difficulty: easy. Tags: arrays, hashmaps.
 */
fun twoSum(numbers: List<Int>, target: Int): Pair<Int, Int> = TODO("implement me")
