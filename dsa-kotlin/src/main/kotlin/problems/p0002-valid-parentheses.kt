package problems

/**
 * Return true when every opening bracket in a string is closed by the same
 * bracket type in the correct order. The input contains only (), [], and {}.
 * For example, "([])" is valid while "([)]" is not.
 * Constraints: 0 <= input.length <= 10_000.
 * Difficulty: easy. Tags: stacks, strings.
 */
fun isValidParentheses(input: String): Boolean = TODO("implement me")
